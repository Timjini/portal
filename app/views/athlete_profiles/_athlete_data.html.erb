    <turbo-frame id="accordion-frame">
        <div id="accordion-collapse" data-accordion="collapse">
                    <!-- LEVEL -->
                    <% @levels.each do |level|%>
                    <h2 id="accordion-collapse-heading-<%=level.id%>">
                        <button type="button" class="flex items-center justify-between w-full p-5 font-medium text-left  border border-b-0  rounded-t-xl " data-accordion-target="#accordion-collapse-body-<%=level.id%>" aria-expanded="true" aria-controls="accordion-collapse-body-<%=level.id%>">
                            <span class="" style="font-size:2rem;"><%= level.title%></span>
                            <svg data-accordion-icon class="w-3 h-3 rotate-180 shrink-0" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 10 6">
                            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5 5 1 1 5"/>
                            </svg>
                        </button>
                    </h2>
                    <div id="accordion-collapse-body-<%=level.id%>" class="hidden" aria-labelledby="accordion-collapse-heading-<%=level.id%>">
                        <div class="p-5 border border-b-0 ">
                        <turbo-frame id="checkbox-frame">
                            <% level.check_lists.each do |checklist| %>
                            <div class="flex items-center mb-4">
                                    <input data-user-id="<%= @athlete.user_id %>" data-checkbox-target="checklist-<%= checklist.id %>" data-checklist-id="<%= checklist.id %>" type="checkbox" value="" class="w-4 h-4 " >
                                    <label for="checklist-<%= checklist.id %>" class="ml-2 text-sm font-medium "><%= checklist.title %></label>
                            </div>
                            <% end %>
                        </turbo-frame>
                        </div>
                        <div>
                            <form class="mt-2 mb-3">
                            <label for="chat" class="sr-only">Your message</label>
                            <div class="flex items-center  py-2 rounded-lg ">
                                <textarea id="chat" rows="1" class="block mx-4 p-2.5 w-full text-sm  rounded-lg border" placeholder="Your message..."></textarea>
                                <button type="submit" class="inline-flex justify-center p-2  rounded-full cursor-pointer ">
                                    <svg class="w-5 h-5 rotate-90" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 18 20">
                                        <path d="m17.914 18.594-8-18a1 1 0 0 0-1.828 0l-8 18a1 1 0 0 0 1.157 1.376L8 18.281V9a1 1 0 0 1 2 0v9.281l6.758 1.689a1 1 0 0 0 1.156-1.376Z"></path>
                                    </svg>
                                    <span class="sr-only">Send message</span>
                                </button>
                            </div>
                            </form>
                        </div>
                        <div>
                        </div>
                    </div>
                    <%end%>
                    
        </div>
    </turbo-frame>
